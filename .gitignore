node_modules/
dist/
.cache/
.tools/
test-results/
playwright-report/
*.local
.DS_Store
.wrangler/

# ML training data (raw/intermediate — not needed in CI)
data/

# ML models — track promotion records, meta, and pkl; ignore OOF/fold/baseline dumps
models/baselines_*.json
models/oof_*.csv
models/folds_*.json

# Vim swap files
*.swp
*.swo

# Temporary command output (redirect workaround for Cline)
.tmp/
